version: "3.8"

services:
  frontend:
    build:
      context: .
      args:
        - VITE_API_BASE_URL=http://teslaeducationbackend-app-1:8000
    environment:
      - VITE_API_BASE_URL=http://teslaeducationbackend-app-1:8000
    ports:
      - "3001:3001"
    networks:
      - teslaeducationbackend_backend-net
    container_name: teslaeducationadmin-frontend

networks:
  teslaeducationbackend_backend-net:
    external: true
